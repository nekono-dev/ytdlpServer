# yt-dlp システム設定 (/etc/yt-dlp.conf)
# JS チャレンジ(EJS)の実行環境。既定の deno は使えないため node を指定する。
--js-runtimes node
# PO Token プロバイダ(bgutil)の接続先。docker-compose の pot-provider サービスを指す。
--extractor-args "youtubepot-bgutilhttp:base_url=http://pot-provider:4416"
